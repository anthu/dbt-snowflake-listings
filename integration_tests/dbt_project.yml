name: 'dbt_snowflake_listings_integration_tests'
version: '0.2.1'
config-version: 2

profile: 'integration_tests'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

models:
  dbt_snowflake_listings_integration_tests:
    staging:
      +materialized: table
      +transient: true
    listings:
      +materialized: organization_listing
