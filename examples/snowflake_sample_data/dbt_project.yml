name: 'snowflake_sample_data_listing_example'
version: '0.1.0'
config-version: 2

profile: 'example'

model-paths: ["models"]
macro-paths: ["macros"]

models:
  snowflake_sample_data_listing_example:
    staging:
      +materialized: table
      +transient: true
    semantic:
      +materialized: semantic_view
    listings:
      +materialized: organization_listing
